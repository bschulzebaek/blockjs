import './autoload-subscribers';
import './BlockJS';
import './framework/state-machine';
import './interface';

/**
 * For saving: abstract WorldDataStorage into Storage class that can be reused for different data structures and can have different intervals
 * 
 * For consistency: Pipe data into a worker and have it save in the background
 * 
 * Implement proper entity system
 */
